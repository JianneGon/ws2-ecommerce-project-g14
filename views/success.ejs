<!DOCTYPE html>
<html>
<head>
  <title><%= title %></title>
</head>
<body>
  <div class="verify-container">
    <h2><%= title %></h2>
    <p><%- message %></p>

    <% if (backLink && backText) { %>
<p><a href="<%= backLink %>" class="btn-back"><%= backText %></a></p>
    <% } %>
  </div>
</body>
</html>
